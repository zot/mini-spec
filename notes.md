we're refining the mini-spec skill's example files.

add traceability and gap docs

The .claude/skills/mini-spec/example needs traceability -- each component

# a
we're working on a light-weight design skill. The goal: produce the highest quality results using the lowest tokens!

The `scratch` directory contains design and code for a contacts app in the style of the `~/work/ui-engine` project. See ui-engine's specs/main.md and other files for information on how it works.

We need to produce `scratch/contacts.md`, a human-readable requirements doc for the app

# b

- convert this to a regular typescript app that runs in a browser in the `ts-example` directory. 
